# 构建上下文排除：加速构建 + 防止密钥/运行时数据被烤进镜像

# VCS / CI（不需要进镜像）
.git
.github
.gitignore
.gitattributes

# 构建/产物/依赖缓存
dist
build
node_modules
**/__pycache__
**/*.pyc
*.egg-info/

# 测试 / 文档 / 运行时数据（绝不进镜像）
tests
docs
data
pro_data
logs
*.log

# 密钥与运行时配置（绝不进入镜像）
dbc_config.json
ai_config.json
.env
.db_key
notifier_config.json
instances.json

# 驱动目录：本地仓库 drivers/ 随构建上下文全部打进镜像（含各 JDBC 驱动 jar / yashandb /
# sqlserver / oracle_client 等）。oracle_client 体积较大，但为支持 Oracle 全功能选择一并打包，
# 接受镜像体积随之增大。
# 注：Docker 镜像仅运行于 Linux，剔除 Oracle instant client 的 windows_x64 平台文件以减小体积。
drivers/oracle_client/windows_x64


# OS 噪声
.DS_Store
Thumbs.db
