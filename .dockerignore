# 构建上下文排除：加速构建 + 防止密钥/运行时数据被烤进镜像

# VCS / CI（不需要进镜像）
.git
.github
.gitignore
.gitattributes

# 构建/产物/依赖缓存
dist
build
node_modules
**/__pycache__
**/*.pyc
*.egg-info/

# 测试 / 文档 / 运行时数据（绝不进镜像）
tests
docs
data
pro_data
logs
*.log

# 密钥与运行时配置（绝不进入镜像）
dbc_config.json
ai_config.json
.env
.db_key
notifier_config.json
instances.json

# 驱动目录：build 阶段从 GitHub Releases 下载 drivers.zip，本地仓库 drivers/ 不进构建上下文
# 例外：Oracle JDBC 驱动不允许公开下载，必须打包进镜像，故保留 ojdbc 两个 jar
drivers/
!drivers/ojdbc6.jar
!drivers/ojdbc8.jar

# OS 噪声
.DS_Store
Thumbs.db
